name: Run Python tests
#
# Sources:
# - https://github.com/marketplace/actions/run-pytest
# - https://github.com/marketplace/actions/install-poetry-action
#

on: [push]

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    env:
      DEBUG: on
      DJANGO_ENV: test
    strategy:
      matrix:
        #
        # Using Matrix to be easier to introduce next version.
        # Generally one version supported at a time.
        #
        python-version: ["3.12"]

    steps:
      - uses: szenius/set-timezone@v1.2
        with:
          timezoneLinux: "Europe/London"
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true
      - name: Install dependencies
        run: make install
#      - name: Install additional dependencies
#        run: pip install pytest-md pytest-emoji
#      - uses: pavelzw/pytest-action@v2
#        with:
#          emoji: false
#          verbose: false
#          job-summary: true
      - name: Run tests
        run: make test
