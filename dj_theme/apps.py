"""App configuration for dj_theme app."""
from django.apps import AppConfig


class DjThemeConfig(AppConfig):
    """App configuration for dj_theme app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "dj_theme"
