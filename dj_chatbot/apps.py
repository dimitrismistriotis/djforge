"""App configuration for the dj_chatbot application."""

from django.apps import AppConfig


class DjChatbotConfig(AppConfig):
    """App configuration for the dj_chatbot application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "dj_chatbot"
