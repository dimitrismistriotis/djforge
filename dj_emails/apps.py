"""App configuration for dj_emails app."""

from django.apps import AppConfig


class DjEmailsConfig(AppConfig):
    """App configuration for dj_emails app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "dj_emails"
